# Baralai High School SQL

Use `supabase.sql` as the complete latest schema/migration file.

Optional helper:

- `cleanup_class_9_10_general_students.sql` helps you review old Class 9/10 students still stored as `General` and assign them to Science/Arts/Commerce safely.

Do not run old migration files from earlier versions; they were merged into `supabase.sql`.
